{
  "name": "node-ffmpeg-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ZX-based CLI tools for working with videos via ffmpeg",
  "bin": {
    "node-ffmpeg-tools": "bin/cli.mjs"
  },
  "scripts": {
    "prepare": "node -e \"process.platform === 'darwin' ? process.exit(0) : process.exit(1)\" && chmod +x bin/cli.mjs || echo \"Skipping chmod on non-macOS systems\""
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "lodash-es": "^4.17.21",
    "puppeteer": "^21.0.0",
    "zx": "^8.8.4"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
